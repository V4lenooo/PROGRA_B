#include <stdio.h>
#include "colas.h"

void Cuenta_X(Tcola ,Tcola , TelementoC , int *);

int main(){
    Tcola Cola_1
    int cont;
    TelementoC X;

    printf("ingrese X\n");
    scanf("%d", &X);

    ("%d aparece %d veces", X, cont);

    return 0;
}

void Cuenta_X(Tcola *Cola_1, TelemC X, int &cont){
    TelementoC elem;
    Tcola Cola_aux
    *cont = 0;
    
    while (!VaciaC(*Cola_1)){
        sacaC(*Cola_1, &elem)

        if (elem == x)
            (*cont)++;
        
        PoneC(*Cola_aux, elem)
    }
    while (!VaciaC(aux)){
        sacaC(&aux, &num);
        poneC(C, num);
}